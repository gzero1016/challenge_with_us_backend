<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dogtiger.challus.repository.UserMapper">

    <update id="updatePassword" parameterType="com.dogtiger.challus.entity.User">
        update user_tb
        set
            password = #{password}
        where
            userId = #{userId}s
    </update>

    <update id="updateNickname">
        update user_tb
        set
            nickname = #{nickname}
        where
            userId = #{userId}
    </update>

    <select id="findUserByOauth2Id" resultType="com.dogtiger.challus.entity.User">
        select
            user_id,
            email,
            name,
            phone,
            nickname,
            password,
            point,
            intro,
            membership,
            is_admin,
            oauth2_id
        from
            user_tb
        where
            oauth2_id = #{oauth2Id}
    </select>
</mapper>